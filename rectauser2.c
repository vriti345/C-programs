#include <stdio.h>
int main()
{
    int len, bred;
    printf("enter the length \n");
    scanf("%d", &len);
    printf("enter the length\n");
    scanf("%d", &bred);
    printf("the area of rect is %d\n", len*bred);
    int a=5;
    printf("%d %d %d",a, a++, ++a);
    return 0;
}